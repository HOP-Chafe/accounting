HOP Chafe' Accounting plugins

Active Bootstrap version: 5.3.8
Path: plugins/bootstrap/

The supplied plugins bundle is preserved in this folder. It also contains an older Bootstrap 3 package at plugins/bootstrap-legacy/, which is intentionally NOT loaded by the application to avoid Bootstrap version conflicts.
